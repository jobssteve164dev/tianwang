# 天网AI网络安全监控系统 - 测试部署指南

## 📋 目录
1. [环境要求](#环境要求)
2. [快速测试](#快速测试)
3. [本地开发环境测试](#本地开发环境测试)
4. [Docker容器化测试](#docker容器化测试)
5. [生产环境部署](#生产环境部署)
6. [故障排除](#故障排除)

## 🔧 环境要求

### 基础环境
- **Node.js**: v18.0.0 或更高版本
- **npm**: v8.0.0 或更高版本
- **Docker**: v20.0.0 或更高版本
- **Docker Compose**: v2.0.0 或更高版本

### 可选环境
- **PostgreSQL**: v15.0.0 或更高版本（本地开发）
- **Redis**: v7.0.0 或更高版本（本地开发）

## 🚀 快速测试

### 1. 运行快速测试脚本
```bash
# 检查系统基本配置和文件结构
node scripts/quick-test.js
```

**预期结果**: 所有25项测试通过，成功率100%

### 2. 检查环境
```bash
# 检查Node.js版本
node --version

# 检查npm版本
npm --version

# 检查Docker版本
docker --version

# 检查Docker Compose版本
docker-compose --version
```

## 💻 本地开发环境测试

### 1. 安装依赖
```bash
# 安装后端依赖
cd server
npm install

# 安装前端依赖
cd ../client
npm install
```

### 2. 配置环境变量
```bash
# 复制测试环境配置
cp server/config/test.env server/.env
```

### 3. 启动本地数据库（可选）
```bash
# 启动PostgreSQL（如果已安装）
brew services start postgresql

# 启动Redis（如果已安装）
brew services start redis
```

### 4. 运行完整测试
```bash
# 运行自动化测试部署
node scripts/test-deployment.js
```

**测试内容**:
- 后端服务启动测试
- 前端应用启动测试
- API健康检查测试
- 前端页面访问测试

**预期结果**:
- 后端服务在 http://localhost:8000 启动
- 前端应用在 http://localhost:3000 启动
- 所有API测试通过
- 前端页面正常加载

### 5. 手动测试
```bash
# 启动后端服务（新终端）
cd server
npm run dev

# 启动前端服务（新终端）
cd client
npm start
```

**访问地址**:
- 前端应用: http://localhost:3000
- 后端API: http://localhost:8000
- API文档: http://localhost:8000/api/docs
- 健康检查: http://localhost:8000/api/health

**测试账户**:
- 用户名: admin
- 密码: 123456

## 🐳 Docker容器化测试

### 1. 启动基础设施服务
```bash
# 启动数据库、缓存、消息队列
docker-compose up -d postgres influxdb redis kafka zookeeper
```

### 2. 启动应用服务
```bash
# 启动后端服务
docker-compose up -d server

# 启动前端服务
docker-compose up -d client

# 启动AI引擎
docker-compose up -d ai-engine
```

### 3. 启动监控服务
```bash
# 启动监控系统
docker-compose -f docker-compose.monitoring.yml up -d
```

### 4. 检查服务状态
```bash
# 查看所有容器状态
docker-compose ps

# 查看服务日志
docker-compose logs server
docker-compose logs client
```

### 5. 访问服务
- 前端应用: http://localhost:3000
- 后端API: http://localhost:8000
- Grafana监控: http://localhost:3001 (admin/admin123)
- Prometheus: http://localhost:9090
- AlertManager: http://localhost:9093

## 🏭 生产环境部署

### 1. 环境准备
```bash
# 创建生产环境配置
cp server/config/test.env server/.env.production

# 修改生产环境配置
# 设置正确的数据库连接信息
# 配置SSL证书路径
# 设置生产环境密钥
```

### 2. 构建生产镜像
```bash
# 构建所有服务镜像
docker-compose build

# 或者单独构建
docker-compose build server
docker-compose build client
docker-compose build ai-engine
```

### 3. 启动生产服务
```bash
# 使用生产环境配置启动
NODE_ENV=production docker-compose up -d
```

### 4. 配置反向代理
```bash
# 配置Nginx反向代理
# 复制SSL证书到 docker/nginx/ssl/
# 修改 docker/nginx/nginx.conf 中的域名配置
```

### 5. 启动监控和备份
```bash
# 启动监控系统
docker-compose -f docker-compose.monitoring.yml up -d

# 配置备份任务
# 添加到crontab
0 2 * * * cd /path/to/tianwang && node scripts/backup/backup-manager.js backup daily
```

## 🔍 故障排除

### 常见问题

#### 1. 端口冲突
```bash
# 检查端口占用
lsof -i :3000
lsof -i :8000

# 停止占用端口的进程
kill -9 <PID>
```

#### 2. Docker服务无法启动
```bash
# 检查Docker服务状态
docker info

# 重启Docker服务
# macOS: 重启Docker Desktop
# Linux: sudo systemctl restart docker
```

#### 3. 数据库连接失败
```bash
# 检查数据库服务状态
docker-compose ps postgres

# 查看数据库日志
docker-compose logs postgres

# 重新初始化数据库
docker-compose down -v
docker-compose up -d postgres
```

#### 4. 前端构建失败
```bash
# 清理缓存
cd client
rm -rf node_modules package-lock.json
npm install

# 重新构建
npm run build
```

#### 5. 监控服务无法访问
```bash
# 检查监控服务状态
docker-compose -f docker-compose.monitoring.yml ps

# 查看监控服务日志
docker-compose -f docker-compose.monitoring.yml logs prometheus
docker-compose -f docker-compose.monitoring.yml logs grafana
```

### 性能优化

#### 1. 数据库优化
```bash
# 运行数据库索引优化
cd server
npm run db:migrate

# 检查数据库性能
docker exec -it tianwang-postgres psql -U tianwang -d tianwang -c "SELECT * FROM pg_stat_user_tables;"
```

#### 2. 缓存优化
```bash
# 检查Redis缓存状态
docker exec -it tianwang-redis redis-cli -a tianwang123 info memory

# 清理缓存
docker exec -it tianwang-redis redis-cli -a tianwang123 flushall
```

#### 3. 负载测试
```bash
# 运行性能测试
node tests/performance/load-test.js

# 查看测试报告
cat tests/performance/load-test-report.json
```

## 📊 监控和维护

### 1. 系统监控
- **Grafana**: 查看系统指标和性能图表
- **Prometheus**: 收集和存储监控数据
- **AlertManager**: 处理告警通知

### 2. 日志管理
```bash
# 查看应用日志
docker-compose logs -f server
docker-compose logs -f client

# 查看系统日志
docker-compose logs -f nginx
```

### 3. 备份管理
```bash
# 创建备份
node scripts/backup/backup-manager.js backup daily

# 列出备份
node scripts/backup/backup-manager.js list

# 恢复备份
node scripts/backup/backup-manager.js restore <backup-file>
```

### 4. 健康检查
```bash
# 运行健康检查
node scripts/monitoring/health-check.js

# 查看性能监控
node scripts/monitoring/performance-monitor.js
```

## 🎯 测试检查清单

### 基础功能测试
- [ ] 用户登录/注册
- [ ] 设备管理
- [ ] 安全事件监控
- [ ] 威胁检测
- [ ] 告警通知
- [ ] 报告生成

### 性能测试
- [ ] API响应时间 < 200ms
- [ ] 并发用户支持 > 1000
- [ ] 数据库查询优化
- [ ] 缓存命中率 > 80%

### 安全测试
- [ ] SQL注入防护
- [ ] XSS攻击防护
- [ ] 速率限制
- [ ] 数据加密
- [ ] 权限控制

### 运维测试
- [ ] 服务自动重启
- [ ] 监控告警
- [ ] 备份恢复
- [ ] 日志管理

## 📞 技术支持

如果遇到问题，请：

1. 查看本文档的故障排除部分
2. 检查系统日志和错误信息
3. 运行诊断脚本
4. 联系技术支持团队

**系统状态**: 生产就绪 ✅
**最后更新**: 2025-08-10
**版本**: v1.0.0
